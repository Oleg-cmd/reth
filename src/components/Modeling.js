// adding models from array

export const Modeling = arr => {
    const arrExample = ['./dog.glb', './heli.glb']
    const arrModels = arrExample.map(model => {
        // model initiation to the scene
        // loadGLTFModel(scene, '/dog.glb', {
        //     receiveShadow: false,
        //     castShadow: false,
        // }).then(() => {
        //     setLoading(false)
        //     animate()
        // })
        //  +++ some styling to the boxes or containers
    })
}
